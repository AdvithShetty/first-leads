/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'cdn.hashnode.com', 'res.cloudinary.com', 'cdn.sanity.io', 'cdn.shopify.com'],
  },
}

module.exports = nextConfig
